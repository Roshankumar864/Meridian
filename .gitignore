node_modules/
_site/
.cache/
.DS_Store
*.log
.netlify/

# Lighthouse artefacts are large. The committed evidence is summary.md plus the
# two home-page HTML reports; the raw JSON and the remaining reports are
# regenerated by `npm run lh` and uploaded by CI as build artefacts.
docs/evidence/*.report.json
docs/evidence/product-*.report.html
docs/evidence/pricing-*.report.html
docs/evidence/contact-*.report.html
